# Instalar desde la raíz del repo:  python -m pip install -r requirements.txt

# Versiones fijadas en la sesión 1 (notebook S1, celda 2; verificadas el 2-sep-2026). No cambiarlas: docs/01 §4
langchain==1.3.18
langchain-core==1.6.1
langgraph==1.2.11
langchain-openrouter==0.2.8
langchain-huggingface==1.2.2
sentence-transformers==6.0.1
faiss-cpu==1.15.0
rank-bm25==0.2.2

# Datos (xbrl_facts.parquet y chunks del índice)
pandas==2.3.3
pyarrow==23.0.0

# Notebooks en VS Code/Jupyter y tests
ipykernel
pytest

# El propio paquete (src/agente10k), editable
-e .
